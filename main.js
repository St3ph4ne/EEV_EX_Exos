// initialisation de la variable counter
let counter = 10;


// Tant que la variable counter est inférieure ou égale à 100
while(counter <= 100) {
    console.log(counter); // affiche la valeur de counter
    counter++; // incrémente counter
}
